<section class='bg-dark'>
	<div class="container pb-4">
		<footer class="d-flex flex-wrap justify-content-between align-items-center py-3 mb-4">
			<p class="col-md-4 mb-0 text-muted">© 2022
				{{data.domain}}</p>

			<ul class="nav col-md-8 justify-content-end">
				{% for link in data.footerLinks %}
					<li class="nav-item">
						<a class="nav-link px-2 text-muted" href="{{link.url}}">{{link.name}}</a>
					</li>
				{% endfor %}
			</ul>
		</footer>
	</div>
</section>
